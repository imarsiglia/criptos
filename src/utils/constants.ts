import Config from 'react-native-config';

//KEYS DE FUNCIONES
export const KEYS_REACT_QUERY = {
  CRYPTOS: "cryptos",
  CRYPTO: "crypto",
};

export const RESEND_TIMEOUT = 60;

export const LOCALSTORAGE_KEYS = {
  app_theme: "APP_THEME",
};

export const BASE_URL = Config.HOST_BASE_URL;

export const LIMIT_PER_LOAD = 50; // Cantidad mínima por carga